// FEATURES.JS — Docker Features & Services
export const features = {
  id:'features',title:'Features & Services',icon:'🔧',
  subtitle:'Every major Docker feature explained — purpose, how it works, when to use it.',
  tags:['features','images','containers','volumes','networks','registry','cli'],
  meta:['📖 20 min','🔵 All Levels'],
  cards:[
    {title:'Images',icon:'💿',colorClass:'quick-note-card--images',content:'<p><strong>Purpose:</strong> Read-only templates that define what a container will contain.</p><p><strong>How it works:</strong> Built from a Dockerfile, images are composed of layers stacked using OverlayFS. Each instruction (FROM, RUN, COPY) creates a cached layer.</p><p><strong>When to use:</strong> Every time you deploy an app. Build your image once, run it everywhere.</p>',items:['Built from Dockerfiles','Composed of cached layers','Stored in registries (Docker Hub, ECR)','Tagged for versioning: myapp:1.0','Base images: alpine, debian, distroless']},
    {title:'Containers',icon:'📦',colorClass:'quick-note-card--containers',content:'<p><strong>Purpose:</strong> Running instances of images — the actual isolated processes.</p><p><strong>How it works:</strong> Docker creates Linux namespaces (PID, NET, MNT) and cgroups for each container, then runs your application process inside this sandbox.</p><p><strong>When to use:</strong> For running any application — web servers, databases, workers, development environments.</p>',items:['Created from images with docker run','Isolated via namespaces and cgroups','Ephemeral by default','Can be stopped, started, paused, removed','Each gets its own filesystem, network, PID space']},
    {title:'Volumes',icon:'💾',colorClass:'quick-note-card--volumes',content:'<p><strong>Purpose:</strong> Persist data beyond container lifecycle.</p><p><strong>How it works:</strong> Named volumes are managed by Docker in /var/lib/docker/volumes/. Bind mounts map host directories directly. Both bypass the container\'s union filesystem.</p><p><strong>When to use:</strong> Databases, file uploads, configuration, shared data between containers.</p>',items:['Named volumes: managed by Docker','Bind mounts: host path → container path','tmpfs: in-memory only','Survive container removal','Can be shared between containers']},
    {title:'Networks',icon:'🌐',colorClass:'quick-note-card--networking',content:'<p><strong>Purpose:</strong> Enable container-to-container and container-to-host communication.</p><p><strong>How it works:</strong> Docker creates virtual network bridges, assigns IPs, and provides DNS resolution. Containers on the same network can reach each other by name.</p><p><strong>When to use:</strong> Always. Most multi-container apps need networking.</p>',items:['bridge: default isolated network','host: shares host network stack','overlay: multi-host (Swarm/K8s)','none: no networking','Custom networks get automatic DNS']},
    {title:'Docker Hub & Registries',icon:'🏪',colorClass:'quick-note-card--compose',content:'<p><strong>Purpose:</strong> Store and distribute Docker images.</p><p><strong>How it works:</strong> Registries are HTTP APIs that store image layers. Docker Hub is the default public registry. Private registries (ECR, GCR, ACR) add auth and scanning.</p><p><strong>When to use:</strong> Sharing images across machines, teams, CI/CD pipelines.</p>',items:['Docker Hub: default public registry','100K+ official and community images','Private registries for organizations','docker push/pull for transfer','Image scanning and signing available']},
    {title:'Docker CLI',icon:'⌨️',colorClass:'quick-note-card--commands',content:'<p><strong>Purpose:</strong> Command-line interface to interact with Docker.</p><p><strong>How it works:</strong> The CLI sends REST API calls to the Docker Daemon. Commands are grouped by object: container, image, volume, network, system.</p><p><strong>When to use:</strong> Always — it\'s the primary way to interact with Docker.</p>',items:['docker container: manage containers','docker image: manage images','docker volume: manage volumes','docker network: manage networks','docker system: system-wide commands','docker compose: multi-container apps']}
  ]
};
