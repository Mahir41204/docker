// GLOSSARY.JS — Docker terminology definitions
export const glossary = {
  id:'glossary',title:'Glossary',icon:'📖',
  subtitle:'Key Docker and containerization terms defined in one line each.',
  tags:['glossary','definitions','terminology','reference'],
  meta:['📖 5 min','🟢 Reference'],
  itemType: 'glossary',
  items:[
    {term:'Container',definition:'A lightweight, standalone, executable package of software that includes everything needed to run an application — code, runtime, system tools, libraries, and settings.'},
    {term:'Image',definition:'A read-only template used to create containers, built from a Dockerfile and composed of stacked layers.'},
    {term:'Dockerfile',definition:'A text file containing sequential instructions to build a Docker image, where each instruction creates a new layer.'},
    {term:'Volume',definition:'A Docker-managed storage mechanism that persists data beyond the lifecycle of a container.'},
    {term:'Bind Mount',definition:'A direct mapping of a host directory or file into a container, giving the container access to host filesystem paths.'},
    {term:'Layer',definition:'A single, read-only modification to an image filesystem, created by each Dockerfile instruction (FROM, RUN, COPY, etc.).'},
    {term:'Registry',definition:'A storage and distribution system for Docker images (e.g., Docker Hub, Amazon ECR, Google GCR).'},
    {term:'Docker Hub',definition:'The default public registry for Docker images, hosting 100,000+ official and community images.'},
    {term:'Docker Daemon',definition:'The background service (dockerd) that manages Docker objects — images, containers, networks, and volumes.'},
    {term:'Docker CLI',definition:'The command-line interface that sends REST API requests to the Docker Daemon to manage containers.'},
    {term:'Docker Compose',definition:'A tool for defining and running multi-container applications using a declarative YAML file (compose.yaml).'},
    {term:'Namespace',definition:'A Linux kernel feature that isolates container processes — providing separate PID, network, mount, IPC, UTS, and user spaces.'},
    {term:'cgroup',definition:'Control Group — a Linux kernel feature that limits, accounts for, and isolates resource usage (CPU, memory, I/O) of processes.'},
    {term:'containerd',definition:'An industry-standard container runtime (CNCF project) used by Docker and Kubernetes to manage container lifecycle.'},
    {term:'runc',definition:'A low-level OCI-compliant container runtime that actually creates and runs containers by interfacing with the Linux kernel.'},
    {term:'OCI',definition:'Open Container Initiative — the industry standard specification for container image formats and runtime behavior.'},
    {term:'OverlayFS',definition:'A union filesystem used by Docker to efficiently layer multiple read-only image layers with a writable container layer on top.'},
    {term:'Bridge Network',definition:'The default Docker network mode that creates an isolated virtual network bridge, connecting containers on the same host.'},
    {term:'Host Network',definition:'A Docker network mode where the container shares the host\'s network stack directly, without isolation.'},
    {term:'Overlay Network',definition:'A Docker network mode for multi-host communication, commonly used in Docker Swarm and Kubernetes.'},
    {term:'Multi-stage Build',definition:'A Dockerfile technique using multiple FROM statements to create smaller production images by separating build and runtime stages.'},
    {term:'Build Context',definition:'The set of files and directories sent to the Docker daemon during a build, defined by the path passed to docker build.'},
    {term:'.dockerignore',definition:'A file that specifies patterns of files and directories to exclude from the Docker build context.'},
    {term:'Orchestration',definition:'The automated management of multiple containers across multiple hosts — handling scaling, networking, health, and deployments (e.g., Kubernetes, Docker Swarm).'},
    {term:'Health Check',definition:'A container-level diagnostic that Docker uses to determine if a service is running correctly and ready to accept traffic.'},
    {term:'Entrypoint',definition:'The executable that runs when a container starts; unlike CMD, it is not easily overridden at runtime.'},
    {term:'CMD',definition:'The default command or arguments passed to the entrypoint when a container starts; easily overridden at runtime.'},
    {term:'EXPOSE',definition:'A Dockerfile instruction that documents which ports the container listens on; does NOT actually publish the port.'},
    {term:'Tag',definition:'A label applied to an image to identify a specific version or variant (e.g., nginx:1.25-alpine).'},
    {term:'Distroless',definition:'Minimal Docker images from Google that contain only the application and its runtime dependencies — no shell, no package manager.'},
    {term:'Alpine',definition:'A lightweight Linux distribution (~5MB) commonly used as a Docker base image for smaller container sizes.'},
    {term:'Swarm',definition:'Docker\'s built-in container orchestration tool for managing multi-node clusters (largely superseded by Kubernetes).'},
    {term:'Service',definition:'In Docker Compose, a service defines a container configuration — its image, ports, volumes, environment, and dependencies.'},
    {term:'tmpfs',definition:'A temporary filesystem stored in memory only — data is never written to disk and is lost when the container stops.'},
    {term:'PID 1',definition:'The first process in a container. It must handle signals properly (SIGTERM) for graceful shutdown — use tini or dumb-init.'}
  ]
};
